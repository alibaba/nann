put app related config files here, 

eg. supervisord app config, contains [program:x] section(required):
    supervisor_$appname.conf

    app requirements(required):
    requirements.txt

    uwsgi config(optional):
    uwsgi.ini

    gunicorn config(optional):
    gunicorn.conf

    app specify config(optional):
    $appname.conf
